// Các hằng số dùng chung cho toàn hệ thống

const USER_STATUS = {
    ACTIVE: "active",
    INACTIVE: "inactive",
    BANNED: "banned",
};

const USER_ROLE = {
    USER: "user",
    // Có thể bổ sung thêm các role khác nếu cần
};

module.exports = {
    USER_STATUS,
    USER_ROLE,
};
